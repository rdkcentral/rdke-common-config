DISTRO_FEATURES:append = ' alsa'
DISTRO_FEATURES:append = ' apparmor'
DISTRO_FEATURES:append = ' argp'
DISTRO_FEATURES:append = ' aslr'
DISTRO_FEATURES:append = ' bind'
DISTRO_FEATURES:append = ' bluetooth'
DISTRO_FEATURES:append = ' bluez5'
DISTRO_FEATURES:append = ' cgroup'
DISTRO_FEATURES:append = ' cobalt-plugin'
DISTRO_FEATURES:append = ' DOBBY_CONTAINERS'
DISTRO_FEATURES:append = ' kirkstone'
DISTRO_FEATURES:append = ' fwupgrader'
DISTRO_FEATURES:append = ' gstreamer1'
DISTRO_FEATURES:append = ' gstreamer1_18'
DISTRO_FEATURES:append = ' ipv4'
DISTRO_FEATURES:append = ' ipv6'
DISTRO_FEATURES:append = ' largefile'
DISTRO_FEATURES:append = ' link_localtime'
DISTRO_FEATURES:append = ' logbacktrace'
DISTRO_FEATURES:append = ' logendpoints'
DISTRO_FEATURES:append = ' multiarch'
DISTRO_FEATURES:append = ' opencdm'
DISTRO_FEATURES:append = ' opengl'
DISTRO_FEATURES:append = ' pam'
DISTRO_FEATURES:append = ' pulseaudio'
DISTRO_FEATURES:append = ' rdm'
DISTRO_FEATURES:append = ' rdm-packager'
DISTRO_FEATURES:append = ' rrd'
DISTRO_FEATURES:append = ' safec'
DISTRO_FEATURES:append = ' ssl-1.1.1'
DISTRO_FEATURES:append = ' subtec'
DISTRO_FEATURES:append = ' syslog-ng'
DISTRO_FEATURES:append = ' systemd'
#Build systemd230 until resolving boot issue with systemd250
DISTRO_FEATURES:append = ' systemd230'
DISTRO_FEATURES:append = ' systemd-journal-cache'
DISTRO_FEATURES:append = ' systemd-profile'
DISTRO_FEATURES:append = ' systimemgr'
DISTRO_FEATURES:append = ' tigress'
DISTRO_FEATURES:append = ' wayland'
DISTRO_FEATURES:append = ' webconfig_bin'
DISTRO_FEATURES:append = ' webkitbrowser-plugin'
DISTRO_FEATURES:append = ' wifi'
DISTRO_FEATURES:append = ' wpeframework'
DISTRO_FEATURES:append = ' disable_security_agent'
DISTRO_FEATURES:append = ' xattr'
DISTRO_FEATURES:append = ' enable_rialto'

DISTRO_FEATURES:append = ' thunder_startup_services'
DISTRO_FEATURES:append = ' wpeframework_systemd_notify'

DISTRO_FEATURES:append = ' gamepad-using-libmanette'

# RDK-E Topic simplification RDK-43306 remove gperftools
DISTRO_FEATURES:append = ' debugtools_disable_gpertools'
DISTRO_FEATURES:append = ' debugtools_disable_gdb'
 
DISTRO_FEATURES:append = ' enable_ripple'

DISTRO_FEATURES:append = ' ctrlm'
DISTRO_FEATURES:append = ' enable_maintenance_manager'
DISTRO_FEATURES:append = ' rdk_svp'
DISTRO_FEATURES:append = ' thunder_security_disable'
DISTRO_FEATURES:append = ' uhd_enabled'
DISTRO_FEATURES:append = ' jspp'
DISTRO_FEATURES:append = ' pwrmgr-plugin'
DISTRO_FEATURES:append = ' enable_libsoup3'
DISTRO_FEATURES:append = ' memcapture'

